# Server Configuration
PORT=8080

# Microsoft Entra ID Configuration
TENANT_ID=your-tenant-id-here
CLIENT_ID=your-client-id-here
CLIENT_SECRET=your-client-secret-here

# Base URL for the application (used for OAuth callback)
# Development: http://localhost:8080
# Production: https://your-app.azurewebsites.net
BASE_URL=http://localhost:8080

# Session Secret (use a strong random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-session-secret-here

# Redis Session Storage (Optional)
# Set REDIS_ENABLED=true to use Redis for session storage (recommended for production)
# Leave REDIS_ENABLED unset or set to false to use MemoryStore (development only)
REDIS_ENABLED=false
# REDIS_URL=redis://localhost:6379
# REDIS_URL=rediss://your-redis-cache.redis.cache.windows.net:6380,password=your-access-key,abortConnect=false,ssl=true
# REDIS_PREFIX=sess:
